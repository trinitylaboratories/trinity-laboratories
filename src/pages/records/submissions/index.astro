---
import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro';
import { getCollection } from 'astro:content';

import SubmissionIndex from '../../../components/records/SubmissionIndex.astro';

const submissions = await getCollection('submissions');
---

<StarlightPage
  frontmatter={{
    title: 'Submission Register',
    description: 'Register of completed TIRN records accepted by Records Control.',
    pagefind: true,
    head: [
      {
        tag: 'meta',
        attrs: { name: 'robots', content: 'noindex, nofollow, noarchive' },
      },
    ],
  }}
>
  <p>
    Entries in this register have completed originating-office review and records-control intake.
    Registration confirms the record identifier, revision, effective date, and distribution state.
  </p>

  <dl class="submission-process">
    <div>
      <dt>Draft</dt>
      <dd>The originating office completes the current approved form.</dd>
    </div>
    <div>
      <dt>Review</dt>
      <dd>The designated reviewer confirms completeness, classification, and distribution.</dd>
    </div>
    <div>
      <dt>Register</dt>
      <dd>Records Control assigns the effective revision and enters the accepted record.</dd>
    </div>
  </dl>

  <h2>Published submissions</h2>
  <SubmissionIndex submissions={submissions} />
</StarlightPage>

<style>
  .submission-process {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    margin: 2rem 0 2.5rem;
    border: 1px solid #aeb2b4;
  }

  .submission-process div {
    padding: 1rem;
  }

  .submission-process div + div {
    border-left: 1px solid #aeb2b4;
  }

  .submission-process dt {
    color: #0759c7;
    font: 700 0.7rem/1.2 var(--sl-font-mono);
    letter-spacing: 0.07em;
    text-transform: uppercase;
  }

  .submission-process dd {
    margin: 0.45rem 0 0;
    color: #3f4953;
    font-size: 0.86rem;
  }

  @media (max-width: 42rem) {
    .submission-process {
      grid-template-columns: 1fr;
    }

    .submission-process div + div {
      border-top: 1px solid #aeb2b4;
      border-left: 0;
    }
  }
</style>
